/**
 * Este archivo fue utilizado para explicar el concepto de compilador
 * C++ es un lenguaje compilado, en donde se transforma el codigo a lenguaje ensamblador
 * De esta forma, se convierte a lenguaje de maquina posteriormente
 */

#include <iostream> // Header file para input/output
using namespace std; // Nombres para objetos y variables de la standard library

// Punto de entrada de la ejecucion del programa
int main() {
    cout << "Hello World"; // Impresion en la consola del string
    return 0; // Retorno de 0 (en caso de que no hayan errores)
}