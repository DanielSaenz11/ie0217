/**
 * Se crea un puntero de un struct
 * Se coloca el nombre del struct seguido de un * y el nombre del struct correspondiente
 */

#include <iostream>
using namespace std;

// Declaracion del struct
struct temp {
    int i;
    float f;
};

int main() {
    // Declaracion de puntero
    temp *ptr;

    return 0;
}